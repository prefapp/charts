---
{{- /*
Define the RoleBinding resource for the argo-workflows-features chart.
The name of the RoleBinding is dynamically generated using the "argo-workflows-features.dynamic-name" template.
*/ -}}
{{ define "argo-workflows-features.rolebinding" }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "argo-workflows-features.dynamic-name" . }}-rb
  namespace: {{ .root.Release.Namespace }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "argo-workflows-features.dynamic-name" . }}-role
subjects:
  - kind: ServiceAccount
    name: {{ include "argo-workflows-features.dynamic-name" . }}-sa
    namespace: "firestartr"
{{ end }}

{{- /*
Iterate over the features defined in the values file and create a RoleBinding for each feature.
The root context and the current feature are passed to the "argo-workflows-features.rolebinding" template.
*/ -}}
{{- $root := . -}}
{{- range .Values.features }}
  {{- include "argo-workflows-features.rolebinding" (dict "root" $root "feature" .) -}}
{{- end }}
